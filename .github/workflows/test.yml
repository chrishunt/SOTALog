name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: macos-15
    defaults:
      run:
        working-directory: SOTALog
    steps:
      - uses: actions/checkout@v4
      - run: swift test
