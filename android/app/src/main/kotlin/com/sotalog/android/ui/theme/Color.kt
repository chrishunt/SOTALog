package com.sotalog.android.ui.theme

import androidx.compose.ui.graphics.Color

// Okabe-Ito colorblind-safe palette — semantic colors

// Active/editing state
val AppOrange = Color(0.9f, 0.6f, 0.0f)
val AppOrangeDark = Color(1.0f, 0.7f, 0.15f)

// SOTA reference
val AppBlue = Color(0.0f, 0.45f, 0.7f)
val AppBlueDark = Color(0.2f, 0.55f, 0.8f)

// POTA reference / valid state
val AppGreen = Color(0.0f, 0.62f, 0.45f)
val AppGreenDark = Color(0.1f, 0.72f, 0.55f)

// Error/warning (dark orange in Okabe-Ito)
val AppRed = Color(0.8f, 0.4f, 0.0f)
val AppRedDark = Color(0.9f, 0.5f, 0.1f)

// Secondary blue
val AppLightBlue = Color(0.34f, 0.71f, 0.91f)
val AppLightBlueDark = Color(0.44f, 0.78f, 0.95f)

// Accent
val AppPink = Color(0.8f, 0.47f, 0.65f)
val AppPinkDark = Color(0.88f, 0.55f, 0.73f)

// Highlight
val AppYellow = Color(0.94f, 0.89f, 0.26f)
val AppYellowDark = Color(0.97f, 0.93f, 0.4f)

// Surface colors
val LightBackground = Color(0xFFFFFBFE)
val DarkBackground = Color(0xFF1C1B1F)
val LightSurface = Color(0xFFFFFBFE)
val DarkSurface = Color(0xFF1C1B1F)
val LightSurfaceVariant = Color(0xFFE7E0EC)
val DarkSurfaceVariant = Color(0xFF49454F)
